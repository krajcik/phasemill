# Rust profile

- Check ownership and lifetime assumptions, error propagation, unsafe blocks, synchronization, cancellation, and resource cleanup.
- Preserve crate boundaries, feature flags, MSRV, formatting, linting, and established error types.
- Test externally observable behavior and important error paths.
