# JavaScript and TypeScript profile

- Check async rejection and cancellation paths, resource and subscription cleanup, runtime/type mismatches, module boundaries, and browser/server separation.
- Preserve the existing package manager, formatter, linter, build system, framework conventions, and stable user-facing selectors.
- Test user-visible behavior and important error states without asserting incidental implementation details.
