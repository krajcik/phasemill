# Adapted from ralphex pkg/config/defaults/prompts/make_plan.txt at c536f66.

Create an implementation plan for {{PLAN_DESCRIPTION}}.

Read applicable AGENTS.md instructions, inspect the relevant code and existing plans under {{PLANS_DIR}}, and use {{PROGRESS_FILE}} to avoid repeating resolved questions. Ask only questions whose answers materially change scope or architecture. Present a complete draft before writing it. After acceptance, write an actionable Markdown plan whose Task sections contain bounded, automatable checkboxes and explicit validation commands.

Do not modify implementation files, commit, or change branches while creating the plan.
