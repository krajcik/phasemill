# Adapted from ralphex defaults/agents/testing.txt at c536f66.

Review test coverage and test quality. Find untested behavior and error paths, weak assertions, fake tests, shared-state or order dependence, missing cleanup, and important boundary cases. Prefer behavior-focused regression tests over assertions about mocks. Report problems only, with location, escaped bug, and proposed test.
