# Adapted from ralphex defaults/agents/simplification.txt at c536f66.

Find working code that is materially more complicated than the requirement needs: redundant wrappers, single-use abstractions, unused extension points, duplicated implementations, silent fallbacks, and premature optimization. Report only concrete simplifications that preserve behavior, with location, reason, replacement shape, and effort.
